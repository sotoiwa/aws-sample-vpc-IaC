# Ansible

- https://docs.ansible.com/ansible/latest/modules/list_of_cloud_modules.html

## Ansibleのインストール

```shell
pip install ansible
pip install boto
pip install boto3
```

## デプロイ

```shell
ansible-playbook site.yml
```
